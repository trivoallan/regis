# Custom Playbook: {{ cookiecutter.project_name }}

Your new RegiS playbook has been created successfully!

## Next Steps

1. Open `playbook.yaml` and define your security sections and rules.
2. Extend `meta.schema.json` to declare project-specific required metadata fields.
   For example, to require a `PROJECT_ID` field:
   ```json
   {
     "$schema": "https://json-schema.org/draft/2020-12/schema",
     "allOf": [
       { "$ref": "https://regis/schemas/meta/well-known.schema.json" }
     ],
     "properties": {
       "PROJECT_ID": { "type": "string" }
     },
     "required": ["PROJECT_ID"]
   }
   ```
3. Use this playbook with the CLI:
   ```bash
   regis analyze <image_url> --playbook ./ -m PROJECT_ID=PROJ-42
   ```
4. You can also reference this playbook from a RegiS repository configuration.
